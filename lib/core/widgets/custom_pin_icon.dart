import 'package:flutter/material.dart';

class CustomPinIcon extends StatelessWidget {
  const CustomPinIcon({super.key});

  @override
  Widget build(BuildContext context) {
    return const Icon(Icons.pin_drop, size: 30, color: Colors.red);
  }
}
